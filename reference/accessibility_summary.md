# PDF accessibility summary

Generates a simple summary of accessibility issues for a given PDF.

## Usage

``` r
accessibility_summary(file, profile = "ua1")
```

## Arguments

- file:

  PDF file to check.

- profile:

  The validation profile to use. Default to `"ua1"` (recommended).
